# built binary
/hindsight
*.test

# local cache home, if anyone overrides HP_HOME into the tree (they shouldn't)
/.hindsight/

# fleet scratch
/.fleet/
/worktrees/

__pycache__/
*.pyc
.DS_Store

# release artifacts: rebuilt by `make release`, and tracking them would
# perturb the very git tree hash the cache keys on
/dist/
